NOTE:   the website buttons wont work due to CORS policy (prevents a file to open another file)
        to resolve this, run a local server first and then serve the html on that server

open command prompt
go to the CamarinesNorteWebsite folder
type
    python -m http.server 8000
open in web browser 
    http://localhost:8000

ctrl + c to terminate